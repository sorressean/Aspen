#include <Frisk.h>
#include "../src/olcs.h"

namespace FriskTests
{
    DEF_TEST(InitializeOlcs_test);
    DEF_TEST(InitializeOlcs_test)
    {
      BEGIN_TEST(self);
      self.setOption(FRISK_OPTION_PENDING, true);
      /* implement your tests for InitializeOlcs! */
    }

}