#include <Frisk.h>
#include "../src/match.h"

namespace FriskTests
{
    DEF_TEST(FullMatch_test);
    DEF_TEST(FullMatch_test)
    {
      BEGIN_TEST(self);
      TEST_OPTION(self, FRISK_OPTION_PENDING, true);
      /* implement your tests for FullMatch! */
    }

}