#include <Frisk.h>
#include "../../src/scripts/scr_Entity.h"

namespace FriskTests
{
    DEF_TEST(RegisterEntityMethods_test);
    DEF_TEST(RegisterEntityMethods_test)
    {
      BEGIN_TEST(self);
      TEST_OPTION(self, FRISK_OPTION_PENDING, true);
      /* implement your tests for RegisterEntityMethods! */
    }

    DEF_TEST(InitializeEntity_test);
    DEF_TEST(InitializeEntity_test)
    {
      BEGIN_TEST(self);
      TEST_OPTION(self, FRISK_OPTION_PENDING, true);
      /* implement your tests for InitializeEntity! */
    }

}