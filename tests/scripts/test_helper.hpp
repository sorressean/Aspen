#include <Frisk.h>
#include "../../src/scripts/helper.hpp"

namespace FriskTests
{
    DEF_TEST(ContainerToScriptArray_test);
    DEF_TEST(ContainerToScriptArray_test)
    {
      BEGIN_TEST(self);
      self.setOption(FRISK_OPTION_PENDING, true);
      /* implement your tests for ContainerToScriptArray! */
    }

}