#include <Frisk.h>
#include "../src/components.h"

namespace FriskTests
{
    DEF_TEST(CreateComponents_test);
    DEF_TEST(CreateComponents_test)
    {
      BEGIN_TEST(self);
      self.setOption(FRISK_OPTION_PENDING, true);
      /* implement your tests for CreateComponents! */
    }

}