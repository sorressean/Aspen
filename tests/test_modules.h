#include <Frisk.h>
#include "../src/modules.h"

namespace FriskTests
{
    DEF_TEST(InitializeModules_test);
    DEF_TEST(InitializeModules_test)
    {
      BEGIN_TEST(self);
      self.setOption(FRISK_OPTION_PENDING, true);
      /* implement your tests for InitializeModules! */
    }

}