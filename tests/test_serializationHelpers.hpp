#include <Frisk.h>
#include "../src/serializationHelpers.hpp"

namespace FriskTests
{
    DEF_TEST(SerializeList_test);
    DEF_TEST(SerializeList_test)
    {
      BEGIN_TEST(self);
      TEST_OPTION(self, FRISK_OPTION_PENDING, true);
      /* implement your tests for SerializeList! */
    }

    DEF_TEST(DeserializeList_test);
    DEF_TEST(DeserializeList_test)
    {
      BEGIN_TEST(self);
      TEST_OPTION(self, FRISK_OPTION_PENDING, true);
      /* implement your tests for DeserializeList! */
    }

}